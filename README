**What is it**
Ubuntu indicator to start scripts in one click

**How to install**
Copy file "deskopen" to /usr/local/bin/
This scripts will open *.desktop files

# cp deskopen /usr/local/bin
# chmod +x /usr/local/bin/deskopen

** run indicator **
** You need python2.7 or python3 to run this indicator **

# python ind.py

** To modify list on known scripts add them to file ex_list**
